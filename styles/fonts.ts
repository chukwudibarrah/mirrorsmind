import localFont from "next/font/local";

export const hadwick = localFont({
  src: "./fonts/hadwick.woff",
  variable: '--hadwick-font'
});

export const neuecomic = localFont({
  src: "./fonts/neuecomic.ttf",
  variable: '--neuecomic-font'
});
